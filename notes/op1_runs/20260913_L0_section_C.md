# OP1-C section sensitivity (same L0 graph)

Recorded as **OP1-C**, a section ledger on the same L0 graph. Not a third adjacency.

## Claim lock

- Same L0 graph. No new base edges from C (the 12 octahedron edges stay; only which Hurwitz units carry them changes).
- OP1 stays **Open**.
- OP3 not entered.
- Default `gauge_section` remains `min_index`. `max_index` and `max_real` are the other two lifts, not replacements of the graph.
- Do not fold C into slice A.

Claim: **Software fact**. Not a bump of OP1.

OP1-C on the same L0 Model 2 graph. Along occupancy is section-invariant. Inter index lifts: 3 distinct sets among ['min_index', 'max_index', 'max_real']. Slice-A overlap is invariant ([8] of 12). Left-half inter_kept is section-dependent (min_index=max_index=5/12; max_real mixed). Not a third graph. Not a bump of OP1. Do not enter OP3.

| section | n_along | n_inter | along=graph | inter indices=graph | overlap | half inter_kept |
|---|---|---|---|---|---|---|
| `min_index` | 24 | 12 | True | True | 8/12 | 0.4166666666666667 |
| `max_index` | 24 | 12 | True | False | 8/12 | 0.4166666666666667 |
| `max_real` | 24 | 12 | True | False | 8/12 | None |

## What is invariant / what moves

- Along occupancy is section-invariant (24 on all three lifts).
- Gaussian–Farey slice-A overlap stays 8/12. Delaunay sits on the six poles; the section only chooses which Hurwitz units carry those 12 base edges.
- The index lift moves under `max_index` and `max_real`.
- Left-half keep-rate moves only under `max_real` (0 or 3 of 12, not 5/12).

C is a choice of section on a fixed graph. A is a slice overlap on that graph. Keep the two dumps separate (`20260911_L0_structure_group_farey_slice_A.json` vs `20260913_L0_section_C.json`).
